import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Enable static export mode so `next export` works after `next build`
  output: "export",
  // You can add additional Next.js configuration here if needed
};

export default nextConfig;
